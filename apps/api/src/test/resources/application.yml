spring:
  datasource:
    url: jdbc:h2:mem:foundation;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  flyway:
    enabled: true
    schemas: app,smartbi_exchange
    default-schema: app

security:
  jwt:
    enabled: false
    issuer: test
