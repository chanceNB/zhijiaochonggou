# Database Foundation

PostgreSQL is provisioned by the repository-level `docker-compose.yml`.
Flyway creates the empty `app` and `smartbi_exchange` schemas from the API module.
No business tables are defined in T00.
